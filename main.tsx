import React from 'react'
import ReactDOM from 'react-dom/client'
import AssessmentPlatform from './app.tsx'
import './main.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AssessmentPlatform />
  </React.StrictMode>,
)
